#include <stdio.h>
#include <unordered_map>

/**
for(url in URLs)
{
    k = html[url]
    list = duplicates[k]
    duplicates[h] = cons(url, list)
}

for(k in duplicates.keys)
{
    list = duplicates[k]
    print(k, list)
}
*/

int main(int argc, char *argv[])
{
    return 0;
}
